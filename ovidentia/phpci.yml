build_settings:
    ignore:
        - "distrib"
        - "skins"
        - "scripts"
        - "styles"

test:
    php_cpd:
        allow_failures: true
    php_loc:
        allow_failures: true

failure:
  email:
    committer: true